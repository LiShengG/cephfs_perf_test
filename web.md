现在希望做一个功能网页，用于管理实验，监控、记录和分析数据，需要实现内容大致如下。

1. 改造run_distributed_mdtest_final.sh 支持读取实验参数文件 test.conf 按照预定参数进行实验
2. 在 实验参数文件 test.conf 中管理参数，例如 NP_PER_TENANT=64，FILES_PER_PROC=10000，ITERATIONS=3，MDTEST_ARGS="-F -C -T -r -R -u -w 4K -e 4K"
3. 实验任务管理: 新增前端网页 支持参数文件 test.conf 的编辑，并支持实验开始、结束按钮，网页左侧边框栏用于对多个不同实验任务名称的展示管理，支持可折叠。后端记录每次实验名称、参数配置以及结果数据。不使用数据库，记录到本地文本文件中即可
4. 实验数据监控，新增监控http端口，用于对各个节点的http监控数据上报，后端需记录周期性的上报数据并持久化，
5. 改造collect_mds_metrics.sh，将采集的数据进行http上报，每个报文内容除了采集的数据，还需加上 ceph daemon ${mds-name}.asok 命令中的mds-name当作mds rank的实例名称。
6. 实验数据展示，网页支持下拉选择各类监控指标数据选项，根据选项做时序图形绘制，支持一个页面展示多个绘制图形子窗口，每个子窗口展示一个类型指标，但同一窗口需绘制对不同的mds rank上报的相同类型的指标，
7. 不同功能的代码模块化放在不同文件夹下的不同文件

梳理我的上述需求，拆解成多个阶段分步骤实现，向我复述你的实现方案，以及说出需要我补充的信息





技术选型
后端你希望用 Python 还是 Node.js？
前端你接受 React 吗？如果没有要求，我建议 React
部署方式
这个网页和后端是部署在“控制机”上吗？
访问方式是仅本机访问，还是局域网多用户访问？
实验模型
“实验任务名称”是否允许重复？
一次任务是否允许同时存在多个并发 run？
test.conf 格式
你希望继续用 shell KEY=VALUE 格式，还是改成更稳的 .env / INI / JSON？
如果你希望兼容 Bash 直接 source，那就继续用 KEY=VALUE
监控上报协议
各 MDS 节点上报到哪个控制机地址和端口？
是否需要鉴权，例如 token？
上报频率是否固定，还是从配置文件里可配？
指标范围
第一版要展示哪些指标？
例如：req_rate、reply_latency、ino、caps、cpu_pct、rss_kb、read_bytes、write_bytes
如果你不给清单，我会先按 collect_mds_metrics.sh 当前能拿到的数据做一版通用解析
结果数据来源
“实验结果数据”是指 mdtest 输出原文、解析后的摘要，还是两者都要保存？
是否需要网页上直接查看 mdtest 原始日志和结果摘要？
停止语义
点击“结束实验”时，你希望：
仅停止 mdtest
同时停止远端监控采集
并立即归档本次 run
权限与环境
后端运行账户是否有权限执行现有脚本、ssh 到各节点、收集结果？
当前 root@host 免密是否已经配置好？


部署前后端的节点是单独一个节点控制机，需下发采集脚本和上报给客户端和ceph集群mds实例节点并远程启动，我会预先配置实验的客户端主机ssh密钥，配置ceph集群mds实例所在节点密钥。
后端你希望用 Python，前端我不熟悉，可按照你默认，建议不要实验功能即可，没必要采用复杂新技术组件
不同实验参数配置需页面提供自定义重命名按钮（不允许重名），不同配置下可进行多次实验实例，一次实验名称等于参数配置+实验时间，实验实例。参数配置自定义名为一级栏目，实验实例为其下属二级栏目。每次启动实验时支持选择已有参数配置，以及重新自定义生成新的配置之后再启动实验
同一时间只能开启一次实验，实验中不允许再启动试验任务
test.conf 格式采用json格式。

各 MDS 节点上报到控制机，端口按你建议即可
无需鉴权，无需复杂安全控制
上报频率从配置文件里可配

指标清单支持可配置，可统一放在conf，或者另外的json配置，格式兼容collect_mds_metrics.sh当前的风格
“实验结果数据”是指 mdtest 输出原文、解析后的摘要暂时 两者都要保存？代码中给我预留一个标识，便于我后期自行修改控制
需要网页上直接查看 mdtest 原始日志和结果摘要
停止语义：仅停止 mdtest，同时停止远端监控采集，并立即归档本次 run
默认root@host 免密是否已经配置好

分阶段编码实现，如有模糊定义，先对我进行询问确认，再做编码



